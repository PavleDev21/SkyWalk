export const locales = ["hr", "en", "de"]

export const pathnames = {
  "/": "/",
  "/pathnames": {
    hr: "/pathnames",
    en: "/pathnames",
    de: "/pfadnamen",
  },
}

// Use the default: `always`
export const localePrefix = undefined
